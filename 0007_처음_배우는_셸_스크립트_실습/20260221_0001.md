## 처음 배우는 셸 스크립트 실습

### 2026.02.21



#### 1. export 
```
export 는 자식 프로세스에 전달여부를 결정한다.
bash 스크립트 실행은 새로운 bash 프로세스를 생성하므로,
해당 bash 스크립트가 종료되면, export 로 선언한 것도 사라진다.
새 bash 스크립트 프로세스는 실행 스크립트의 자식 프로세스일 수 밖에 없기 때문이다. 
```

### 2. 변수선언
```aiignore
반드시 변수=값 형태로 붙여 써야 한다.
공백 허용되지 않는다.

-- 정상 
FOO=bar

-- 오류
FOO = bar
이 상황을 FOO 를 실행하고 "=" 는 첫번째 인자, bar 는 두번째 인자로 해석한다.
```


### 인텔리제이 열편집
```aiignore
alt + "마우스 드레그"
```

### 3. function 
```aiignore
다른 언어와 다른점 
- 함수명 선언시 인자정보를 넣을 수 없다.

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test0001.sh 
#!/bin/bash

function print() {
    echo $1
    echo $2 
    echo $@
    echo $#    
}

print "hello world"

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 



🔹 왜 함수명 옆에 인자를 못 쓰나
bash는 동적 쉘 언어라서
타입 없음
시그니처 없음
오버로딩 없음

대신 실행 시 전달된 인자를 다음으로 접근합니다:
표현	의미
$1	첫 번째 인자
$2	두 번째 인자
$@	전체 인자 목록
$#	인자 개수

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test0001.sh 
hello world         <- $1    
                    <- $2 , 에러 발생 아님에 주의한다.
hello world         <- $@  
1                   <- $# 
colaman@D3-VM-DEV-00:~/WORKS/bash_study$


로컬변수는 앞에 local 로 선언한다.


colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0002.sh 
#!/bin/bash

language="Korean"

function learn()
{
    local learn_language="English"
    echo "I am learning [$learn_language]"
}

function print() {
    echo "I can speak [$1]"
    # echo $#    
}

learn
print $language
print $learn_language
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0002.sh 
I am learning [English]
I can speak [Korean]
I can speak []
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
```



#### 4. 위치 매개변수
```
$0 실행된 스크립트 이름
$1 $1 $2 $3.. ${10} 파라미터 순서대로 번호가 부여되며, 10번째 부터는 "{}" 로 감싸야 함
$* 전체 인자 값 -> 따옴표 사용시 전체 인자값이 문자열로 변환, 사용주의 필요(사용불필요) 
$@ 전체 인자 값 -> 따옴표 사용시에도 개별 인자값 유지, 전체 인자값 출력시 보통 $@ 사용  
$# 매계변수의 총 개수

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0002.sh 
#!/bin/bash

language="Korean"

function learn()
{
    echo $0
    local learn_language="English"
    echo "I am learning [$learn_language]"
}

function print() {
    echo $0
    echo "I can speak [$1]"
    # echo $#    
}

    
echo $0
learn
print $language
print $learn_language
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0002.sh 
./test_0002.sh
./test_0002.sh
I am learning [English]
./test_0002.sh
I can speak [Korean]
./test_0002.sh
I can speak []
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
```


### 5. 특수매개변수
```aiignore
$$ 현재 스크립트 또는 명령어의 PID
```



### 6. 매개변수 확장
```aiignore
$변수     현재 문자열에서 해당 변수를 파라미터 값으로 치환
${변수}   위와 같지만 {} 를 사용함으로서,뒤에 오는 문자열과 구분 가능 
```


### 7. 조건문 switch-case 문

```aiignore
case $변수 in
    조건값1)
    수행문1 ;;
    조건값2)
    수행문2 ;;
    조건값3)
    수행문3 ;;
    *)
    수행문4        
esac


colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0005.sh 
#!/bin/bash

case $1 in
    start)
    echo "Start"
    ;;
    stop)
    echo "stop"
    ;;
    restart)
    echo "restart"
    ;;
    help)
    echo "help"
    ;;
    *)
    echo "Please input sub command"
esac

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0005.sh 
Please input sub command
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0005.sh start
Start
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0005.sh stop
stop
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0005.sh restart
restart
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0005.sh undefined
Please input sub command
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
```


### 8. 반복문 for 기본사용법1

```aiignore
#기본사용법1
for 변수 in [범위(리스트 또는 배열, 묶음 등)]
do
    반복할 수행문
done


#기본사용법2
for ((변수=초기값; 조건식; 증가값))
do
    반복할 수행문
done




예제1)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0006.sh 
#!/bin/bash

for num in 1 2 3
do
    echo $num
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0006.sh 
1
2
3
colaman@D3-VM-DEV-00:~/WORKS/bash_study$


예제2)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0007.sh 
#!/bin/bash

numbers="1 2 3"
for num in $numbers
do
    echo $num
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0007.sh 
1
2
3
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 


예제3)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0008.sh 
#!/bin/bash

for file in $HOME/WORKS/bash_study/*
do
    echo $file;
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0008.sh 
/home/colaman/WORKS/bash_study/test0001.sh
......
......
/home/colaman/WORKS/bash_study/test_0008.sh
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 


예제4)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0009.sh 
#!/bin/bash

for num in {1..5}
do
    echo $num;
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0009.sh 
1
2
3
4
5
colaman@D3-VM-DEV-00:~/WORKS/bash_study$



예제5)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0010.sh 
#!/bin/bash

for num in {1..10..2}
do
    echo $num;
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0010.sh 
1
3
5
7
9
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ 




예제6)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0011.sh 
#!/bin/bash

array=("apple" "banana" "pineapple")

# 배열을 사용할 때는 ${배열[@]}로 표현해야 배열의 모든 아이템을 사용할 수 있음
for fruit in ${array[@]}
do
    echo $fruit;
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0011.sh 
apple
banana
pineapple
colaman@D3-VM-DEV-00:~/WORKS/bash_study$
```


### 9. 반복문 for 기본사용법2

```
#기본사용법2
for ((변수=초기값; 조건식; 증가값))
do
    반복할 수행문
done

예제1)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0012.sh 
#!/bin/bash

# 기본사용법2를 사용할 경우에는 반드시 ()소괄호를 더블(())로 사용하여야 함
for ((num=0; num<3; ++num))
do
    echo $num;
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0012.sh 
0
1
2
colaman@D3-VM-DEV-00:~/WORKS/bash_study$
```


### 10. 반복문 while 문
```aiignore
#기본사용법
while [ $변수1 연산자 $변수2 ]
do
    반복할 수행문
done


예제)
colaman@D3-VM-DEV-00:~/WORKS/bash_study$ cat ./test_0013.sh 
#!/bin/bash

num=0

while [ $num -lt 3 ]
do
    echo $num
    num=$((num+1))
done

colaman@D3-VM-DEV-00:~/WORKS/bash_study$ ./test_0013.sh 
0
1
2
colaman@D3-VM-DEV-00:~/WORKS/bash_study$


num=$((num+1)) 는 bash 의 산술확장 문법이다.
$(( ... ))
→ 괄호 안을 정수 계산
```

### 11. bash 산술확장 문법
```aiignore
# num=num+1 
은 문자열 "num+1" 을 변수 num 에 저장하는 것이다.
bash 변수는 기본적으로 문자열이다.
산술연산을 해야 한다.
 
 
# num=$((num+1)) 
bash 의 산술확장 문법이다.


핵심부터 말씀드리면:
👉 Bash에서는 num=num+1은 계산이 아니라 문자열 대입입니다.
👉 증가 연산을 하려면 num=$((num+1)) 같은 산술 확장이 필요합니다.

🔹 num=$((num+1)) 해석
이 줄은 Bash의 산술 확장(arithmetic expansion) 문법입니다.
num=$((num+1))

단계별로 보면:

$(( ... ))
→ 괄호 안을 정수 계산

num+1
→ 현재 num 값에 1을 더함

계산 결과를 num= 에 대입

예:
num=0
num=$((num+1))
echo $num

출력:
1
🔹 그럼 num=num+1 은?

이건 계산이 아닙니다.
num=num+1
의 의미는:
👉 문자열 "num+1"을 변수 num에 저장
예:
num=0
num=num+1
echo $num

출력:
num+1

즉, Bash는 자동으로 수식을 계산해주지 않습니다.

🔹 왜 이런 차이가 생길까?
Bash 변수는 기본적으로 문자열입니다.
❌ num=num+1 → 문자열 대입
✅ num=$((num+1)) → 산술 계산 후 대입
Bash는 C언어처럼 자동 계산하지 않습니다.


🔹 증가시키는 올바른 방법들

✅ 방법 1: 가장 권장
num=$((num+1))

✅ 방법 2: 축약형
((num++))

또는

((num+=1))

👉 루프에서는 이것도 매우 많이 사용합니다.

✅ 방법 3: expr (구식)
num=$(expr $num + 1)
⚠️ 요즘은 거의 안 씀 (느림, 불편)

🔹 정리
num=num+1 → ❌ 문자열 대입 (문제 발생)
num=$((num+1)) → ✅ 정수 계산 후 증가
((num++)) → ✅ 간결한 증가
$(( ... ))
→ 괄호 안을 정수 계산
```
